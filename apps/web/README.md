# web

**Status: reserved, intentionally empty.**

No UI milestone is defined. The roadmap runs from the Repository Scanner to AI
Integration without a phase in which anything becomes visible, so no frontend
framework has been chosen or installed.

This directory exists so the documented package structure matches what is on
disk. It will be initialised when a UI milestone is agreed.
